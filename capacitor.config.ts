import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.slinfy.crm',
  appName: 'slinfy-crm-angular',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
